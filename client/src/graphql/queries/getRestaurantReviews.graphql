query getRestaurantReviews($id: String!) {
  restaurant(id: $id) {
    id
    reviews {
      id
      title
      creatorId
      createdAt
      content
      points
      creator {
        username
        id
      }
    }
  }
}
